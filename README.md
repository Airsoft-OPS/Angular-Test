# Angular-OPS-NG

# Instalation 

1. node + npm instalados
2. npm install -g @angular/cli
3. cd 'project-folder'
4. npm install
5. npm install @supabase/supabase-js
6. Add Supabase URL + Key to environment.ts/environment.development.ts
7. ng generate services/service
9. ng serve
